{
  "name": "tdb-history-shadow-tools",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "Shadow-only tooling for building, seeding, and auditing historical TencentDB Agent Memory inputs.",
  "license": "MIT",
  "bin": {
    "tdb-history-inventory": "./src/tdb-history-inventory.mjs",
    "tdb-seed-input-builder": "./src/tdb-seed-input-builder.mjs",
    "tdb-shadow-seed-runner": "./src/tdb-shadow-seed-runner.mjs",
    "tdb-shadow-audit": "./src/tdb-shadow-audit.mjs",
    "tdb-history-shadow": "./src/tdb-history-shadow.mjs"
  },
  "scripts": {
    "scan:secrets": "node scripts/scan-secrets.mjs",
    "test": "node --test",
    "check": "npm run scan:secrets && npm test && node --check src/tdb-history-shadow.mjs && node --check src/tdb-history-lib.mjs && node --check src/tdb-shadow-audit.mjs && node --check src/tdb-shadow-seed-runner.mjs && node --check src/tdb-seed-input-builder.mjs && node --check src/tdb-history-inventory.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
